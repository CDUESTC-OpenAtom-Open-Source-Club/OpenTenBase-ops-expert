# BOOT.md

启动自检清单（仅运行时检查，不创建任何文件）。

## 规则

- **本文件是 Agent 的自我检查清单，不是操作指令。**
- **不创建任何文件、目录、元数据。** 缺失目录/文件不意味着需要"初始化"，只意味着某些功能不可用需告知用户。
- 如果用户没有要求"初始化项目"或"创建工作区"，跳过所有一致性检查。

## 环境检查

- [ ] 如任务需要运行脚本，确认 Python3 可用（`python3 --version`），不可用时告知用户
- [ ] 按用户意图读取对应 `SKILL.md`，不预加载全部技能

## 状态检查

- [ ] 需要连续上下文时，可读取 `memory/` 下已有日志；**不创建新日志文件**
- [ ] 检查目标 Skill 的 references/ 是否需要加载
