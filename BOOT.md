# BOOT.md

> ⚠️ **运行时禁止执行本文件。** 本文件仅供本地开发者手工检查工作区完整性时参考。
> 在 QClaw / 线上运行时，**不要**把下面的清单当成会话启动步骤去执行：不读取 MEMORY.md、不创建 memory 目录、不做一致性自检、不写任何文件。
> 收到用户请求时，请直接按 `AGENTS.md` 的"任务识别与路由"处理用户的实际问题。

启动自检清单（**仅本地开发参考**）。每次会话启动时按顺序检查（可选文件，配合 hooks 使用）。

## 环境检查

- [ ] 如任务需要运行脚本，检查 Python 可用：`python3 --version`
- [ ] 工作区 Skills 已加载
- [ ] `memory/` 目录可读写
- [ ] 如任务涉及远程服务器，确认目标主机、登录用户、是否需要切换到 OpenTenBase 运行用户

## 状态检查

- [ ] 读取 `MEMORY.md` 加载长期记忆
- [ ] 需要连续上下文时，读取 `memory/` 最近日志
- [ ] 检查 `HEARTBEAT.md` 是否有到期任务
- [ ] 按用户意图读取对应 `SKILL.md`，不预加载全部技能

## 一致性检查

- [ ] `IDENTITY.md` 与 `SOUL.md` 定位一致
- [ ] `TOOLS.md` 引用的工具实际可用
- [ ] `skills/` 下每个 Skill 的 `SKILL.md` 格式正确

## 失败处理

某项检查失败时：
1. 告知用户缺少什么，以及会影响哪类任务
2. 不猜测路径或强行执行命令
3. 只有在用户明确要求长期记录时，才写入 `MEMORY.md` 或每日记忆
