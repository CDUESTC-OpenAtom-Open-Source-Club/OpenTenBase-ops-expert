# BOOT.md

启动自检清单。每次会话启动时按顺序检查（可选文件，配合 hooks 使用）。

## 环境检查

- [ ] Python 可用：`python3 --version`
- [ ] 工作区 Skills 已加载
- [ ] `memory/` 目录可读写
- [ ] {{其他环境依赖检查}}

## 状态检查

- [ ] 读取 `MEMORY.md` 加载长期记忆
- [ ] 读取 `memory/` 最近日志恢复上下文
- [ ] 检查 `HEARTBEAT.md` 是否有到期任务
- [ ] {{其他状态检查项}}

## 一致性检查

- [ ] `IDENTITY.md` 与 `SOUL.md` 定位一致
- [ ] `TOOLS.md` 引用的工具实际可用
- [ ] `skills/` 下每个 Skill 的 `SKILL.md` 格式正确

## 失败处理

某项检查失败时：
1. 记录失败项到 `memory/` 当日日志
2. 不阻塞会话，但告知用户缺失的能力
3. 尝试自动修复可恢复项（如创建缺失目录）
