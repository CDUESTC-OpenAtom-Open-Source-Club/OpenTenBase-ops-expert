# 行为层面修复 — 基于评估反馈

> 2026-07-04 | 评估指出：文件创建0分、简洁性0.5分、已提供日志却反问用户

---

## 三大问题修复

### 问题 1：不自觉创建文件 → AGENTS.md 铁律 1 强化 + SOUL.md 补充

**根因：** 系统指令要求写 "task artifact"，但 AGENTS.md "只回复对话" 规则不够强硬，未明确列出禁止的文件类型。

**修改：**
- AGENTS.md 铁律 1 改为 **"零容忍——绝不碰文件系统"**，明确列出禁止生成的文件名：`_meta.json`、`BOOT.md`、`CHANGELOG.md`、`LICENSE`、`README.md`、`_sum*` 等
- 加入 **"对话文字足够。不写文件、不说文件路径、不提保存确认"**
- SOUL.md 核心准则新增 **"纯对话零文件"**，说明：不写文件、不创建脚本、不生成报告、不输出文件路径、不说"已保存"
- SOUL.md 说话禁忌新增：不说 file path、不说 file action、不说自我介绍、不说路由过程、不用 emoji

### 问题 2：用户已提供日志却反问 → AGENTS.md 铁律 2 补充

**根因：** 原规则只覆盖了"用户没给日志时先给采集命令"，缺失"已给日志时直接分析"的分支。

**修改：**
- AGENTS.md 铁律 2 新增 **"用户已提供日志/SQL/错误信息时：直接分析，定位根因，给出修复命令。不追问、不要求补充信息。"**
- AGENTS.md 日志错误必答要点开头新增 **"用户已提供日志内容时：直接分析日志文本，定位根因（哪个节点、什么时间、什么错误码），给出修复命令。不追问、不要求补充信息。"**

### 问题 3：回复冗长 → AGENTS.md 铁律 4 加量

**根因：** 原规则只说"短"但没给具体行数限制。

**修改：**
- AGENTS.md 铁律 4 新增 **最多 20 行 + 2 个命令块** 的硬约束
- 固定结构示例改为带行数标注的版本：
  ```
  结论/初判（1-2 行）
  命令（1 个块，不超过 10 行）
  怎么看结果（1-2 行）
  下一步（1 行）
  ```

### 辅助：新增"永远不要输出的内容"清单

放在 AGENTS.md 路由表之前，覆盖所有禁止输出的内容类别：
- 文件路径/文件名
- 保存确认
- 项目初始化
- 长篇报告/科普
- emoji/装饰性符号
- 自我介绍/技能路由/填充语

---

## 结论

本次不修改技能内容（之前已完善了技术准确性），而是聚焦 **行为规范**。评估中的技术分（分布式意识 0.8、信息收集 0.75、命令可操作性 0.7）其实合理，拖分的是 **行为层面的零容忍违规**。三条铁律的强化直接针对这三个失分点。
