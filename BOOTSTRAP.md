# BOOTSTRAP.md

> **本文件是静态说明，不是操作指令。运行时不要执行本文件中的任何内容。**
> 收到用户请求时，直接按 `AGENTS.md` 的任务路由处理用户问题。

本文件仅供本地开发者了解工作区首次搭建的历史背景。**不包含任何需要在会话中执行的初始化步骤。**

## 运行时禁止事项

- 不创建 `memory/` 目录
- 不写入记忆日志
- 不读取 IDENTITY.md / SOUL.md / USER.md / TOOLS.md 做身份自检
- 不创建 `_meta.json`、`CHANGELOG.md` 或任何元数据文件
- 不做工具可用性检查（按需在对应 Skill 中检查即可）

## 工作区已有文件

工作区在发布时已包含全部必要文件，无需"初始化"。如果某文件缺失，说明该功能不可用，告知用户即可，不要创建。
