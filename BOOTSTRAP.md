# BOOTSTRAP.md

> ⚠️ **运行时禁止执行本文件。** 本文件仅供本地开发者首次搭建工作区时手工参考。
> 在 QClaw / 线上运行时，**不要**执行下面的初始化步骤：不创建 memory 目录、不写记忆日志、不做身份/工具自检。
> 收到用户请求时，直接按 `AGENTS.md` 路由到对应子技能解决问题。

首次初始化说明（**仅本地开发参考**）。正常运行时不需要读取本文件。

## 初始化步骤

### 1. 身份确认

读取 `IDENTITY.md`，确认：
- [ ] Name 已填写
- [ ] Vibe 已填写
- [ ] Emoji 已选择

### 2. 人设定型

读取 `SOUL.md`，确认：
- [ ] 核心准则已填写
- [ ] 边界已明确
- [ ] 表达风格已定义

### 3. 用户档案

读取 `USER.md`，确认：
- [ ] 已设置默认称呼，或按用户要求更新
- [ ] 已记录基本偏好

### 4. 工具就绪

读取 `TOOLS.md`，确认：
- [ ] 引用的工具实际可用
- [ ] 技能目录和 reference 目录存在

### 5. 记忆初始化

- [ ] 创建 `memory/` 目录
- [ ] 需要时写入首条记忆日志
- [ ] `MEMORY.md` 初始状态确认

### 6. 完成清理

- [ ] 自检通过（参考 `BOOT.md`）
- [ ] 本文件可以保留为说明，也可以在完成初始化后删除

## 注意

- 密码、私钥、token、生产库连接串不得写入任何记忆文件。
- 未确认用户、路径、版本、拓扑前，不执行写操作。
