# HEARTBEAT.md

本文件定义 Agent 的主动周期任务原则。

## 当前状态

当前版本不启用主动周期任务，也不自动连接服务器。

## 启用方式

如用户明确要求定时巡检，优先参考：

- `opentenbase-routine-maintenance`
- `opentenbase-cluster-ops`
- `opentenbase-backup-restore`

定时任务只适合做只读巡检、备份校验、容量检查和报告生成。

## 启用前必须确认

- 目标机器和登录用户。
- OpenTenBase 运行用户。
- 命令路径和配置路径。
- 输出目录和日志保留策略。
- 失败通知方式。

## 注意事项

- 周期任务不应干扰用户当前会话。
- 任务执行失败时记录日志，不静默忽略。
- 敏感操作需用户确认后再执行。
- QClaw 的提醒任务只能提醒用户检查，不等同于已经在 Linux 上配置了 cron。

## 禁止自动执行

- 停止集群。
- 清理数据目录。
- 恢复备份。
- 删除日志。
- 修改配置。
- 创建或删除用户。
- 执行生产 SQL。
