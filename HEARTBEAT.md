# HEARTBEAT.md

本文件定义 Agent 的主动周期任务（定时执行的自动化任务）。

## 当前状态

当前版本不启用主动周期任务。

## 启用方式

如需启用，按以下结构定义任务：

### 任务一：{{任务名称}}

- **触发周期**：{{如 每日 / 每小时 / 每周}}
- **触发条件**：{{具体条件}}
- **执行内容**：{{任务描述}}
- **输出方式**：{{如 写入文件 / 发送通知 / 更新状态}}

### 任务二：{{任务名称}}

- **触发周期**：{{周期}}
- **触发条件**：{{条件}}
- **执行内容**：{{描述}}
- **输出方式**：{{方式}}

## 注意事项

- 周期任务不应干扰用户当前会话。
- 任务执行失败时记录日志，不静默忽略。
- 敏感操作需用户确认后再执行。
